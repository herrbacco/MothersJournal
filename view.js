var view = {

  displayEntry : function (entry) {
    console.log('displaying entry: ' + entry);
  },
  displayTags : function (entry) {
    console.log('displaying tags for entry: ' + entry );
  }
}

module.exports = view;
